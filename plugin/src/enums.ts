export enum DURATION_ENUM {
    SHORT = 'short',
    LONG = 'long',
    INDEFINITE = 'indefinite'
}

export enum POSITION_ENUM {
    DEFAULT = 'default',
    CENTER = 'center'
}

export enum TYPE_LINE_ENUM {
    DEFAULT = 'default',
    MULTILINE = 'multiline'
}